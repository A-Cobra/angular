import { CustomizableOption } from '../fast-food-module/models/customizable-option.interface';

export const defaultFormText: CustomizableOption = {
  name: 'Enter special indications for',
  type: 'text',
  required: false,
};
