import { CustomizableOption } from '../fast-food-module/models/customizable-option.interface';

export const defaultSingleSelection: CustomizableOption = {
  name: 'Select the type of',
  type: 'single-select',
  required: true,
};
