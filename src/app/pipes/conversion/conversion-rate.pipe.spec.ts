import { ConversionRatePipe } from './conversion-rate.pipe';

describe('ConversionRatePipe', () => {
  it('create an instance', () => {
    const pipe = new ConversionRatePipe();
    expect(pipe).toBeTruthy();
  });
});
